# 安全政策

## 报告漏洞

请不要在公开 Issue 中披露可利用的漏洞、密钥或个人数据。优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私下提交报告，并包含影响范围、复现条件和建议修复方式。维护者会在确认后协调披露时间。

如果仓库尚未启用 Private vulnerability reporting，请先联系仓库维护者开启该功能，不要改用公开讨论区粘贴敏感细节。

## 支持范围

安全修复以默认分支的最新版本为准。旧部署和个人 Fork 应先更新到最新提交，再确认问题是否仍然存在。

## 密钥与数据

- `OPENAI_API_KEY` 只应保存于 GitHub Actions Secrets 或本地忽略的 `.env.local`。
- 不要把真实密钥写入 `.env.example`、工作流文件、日志、截图、Issue 或提交历史。
- 怀疑密钥泄露时，应立即在对应平台撤销并轮换，而不是只从最新提交中删除。
- GitHub Actions 应使用最小权限；GitHub 与 OpenAI 账号应开启平台提供的安全保护。
- 本项目不应存储公众号关注者名单或不必要的个人数据。

## 自动发布安全

新的早报只有在结构、来源与时间窗口校验通过后才能成为首页最新一期。定时任务失败时应保留上一期；补跑发现当日期刊已经存在时不得再次调用模型。公众号页面只负责生成可复制排版，不持有微信 AppSecret，也不调用群发接口。
